/*
* Query the page to get the color from the user:
    * Hex
    * 0-255
* Validate that color:
    * If invalid, display an error message to the user
    * If valid send to colorrange functions and acquire colors array
* Display a column of color boxes which is the length of the colors array
* Assign a color to each display box according to the contents of the colors array
*/

